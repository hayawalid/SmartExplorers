# SmartExplorers Backend Requirements
# Updated for Windows compatibility

# FastAPI Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0

# Database
sqlalchemy==2.0.25

# Data Validation
pydantic==2.5.3
pydantic-settings==2.1.0

# Utilities
python-multipart==0.0.6
python-dotenv==1.0.0
requests==2.31.0

# AI/ML - OpenAI
openai==1.12.0

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Encryption
cryptography==42.0.0

# Image Processing
Pillow==10.2.0
opencv-python==4.9.0.80

# OCR
pytesseract==0.3.10

# Face Recognition - Windows Compatible Versions
# CRITICAL: NumPy must be < 2.0 for TensorFlow compatibility
numpy<2.0.0

# TensorFlow 2.15 is not available for Windows, use 2.16 instead
tensorflow==2.16.1
tf-keras==2.16.0

# DeepFace for face verification
deepface==0.0.79

# Optional: For PostgreSQL (if not using SQLite)
# psycopg2-binary==2.9.9

# Optional: For Redis (if implementing caching)
# redis==5.0.1

# Note: After installing these, you also need to install Tesseract OCR separately:
# Windows: https://github.com/UB-Mannheim/tesseract/wiki
# Mac: brew install tesseract
# Linux: sudo apt-get install tesseract-ocr