# FastAPI Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0

# Database
sqlalchemy==2.0.25

# Data Validation
pydantic==2.5.3
pydantic-settings==2.1.0

# Utilities
python-multipart==0.0.6
python-dotenv==1.0.0

# AI/ML - OpenAI (UPDATED VERSION)
openai==1.52.0

# HTTP Client - ensure compatibility
httpx==0.27.0

# Optional: Groq SDK (if using Groq instead of OpenAI)
groq>=0.1.0

# Authentication (for future integration with Part 1)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# HTTP Client (for testing)
requests==2.31.0

# Optional: For PostgreSQL (if not using SQLite)
# psycopg2-binary==2.9.9

# Optional: For Redis (if implementing caching)
# redis==5.0.1